# 開発環境用のデータベース設定
# 本番環境ではVercelが自動的に環境変数を設定します
POSTGRES_PRISMA_URL="postgresql://postgres:postgres@localhost:5432/lottery?schema=public"
POSTGRES_URL_NON_POOLING="postgresql://postgres:postgres@localhost:5432/lottery?schema=public"

# DynamoDB設定（開発環境用）
DYNAMODB_ENDPOINT="http://localhost:8000"
AWS_REGION="local"
AWS_ACCESS_KEY_ID="local"
AWS_SECRET_ACCESS_KEY="local" 